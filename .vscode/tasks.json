{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-app",
            "type": "shell",
            "command": "lsof -i :3000 || (export WS_PORT=3000 && npm run start)",
            "runOptions": {
                "runOn": "folderOpen"
            },
        }
    ]
}
