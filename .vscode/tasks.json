{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-app",
            "type": "shell",
            "command": "lsof -i :8080 || (chmod +x start.sh && ./start.sh)",
            "runOptions": {
                "runOn": "folderOpen"
            },
        }
    ]
}
